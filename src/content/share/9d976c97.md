---
title: day2 移動中 initcut pilot — 進度接續（TG 離線前備份）
date: 2026-07-13
creator: robert
co_creators: []
tags:
  - 影片
  - 初剪
  - fcpxml
  - handoff
  - 德國
---
sourceNote: "robert__handoff__2026-07-13_day2移動中_initcut_pilot_進度"

# day2 移動中 initcut pilot — 進度接續

<div class="not-prose my-6 bg-blue-500/10 border-l-4 border-blue-500 rounded-r-lg p-4">
<p class="font-bold text-blue-400 mb-2">📝 背景</p>
<div class="text-sm text-gray-300">

「你選→我調」initcut workflow 的首個 pilot：James 在 FCPX 撿片（Star2 拼貼／Star3-4 剪輯）→ 導出 → Robert 組序＋跨相機色＋GPS 地圖＋logo＋拼貼 grid → 交 FCPXML 給 James 微調。德國 day2「移動中」主題。相關 FCPXML_拼貼grid_transform校正、reference_narrative_tag_tool。

</div>
</div>


## ✅ 現況：已交付並由 James 完成剪輯

- **母帶**：`…/_初剪_day2_移動中/Day2 移動中 初剪 v5 (票卡+地圖動線).fcpxmld`（James 剪好版）
  - 剪到 **~5.5 分**（我給的 ~11 分砍半）、**James 自己套色 126 處**、加了配樂**人生迴旋曲（霍爾主題）**、FCP 存成 1.14。
  - 50 媒體連結全有效＋嵌 bookmark。**備份 = 整個 trip 資料夾一起**（fcpxmld 只是配方不含媒體）。符合收尾 SOP feedback_fcpx_archival_no_prores。
- **我出的 v5 fcpxml**（1.10）：同資料夾 `day2_移動中_初剪.fcpxml`（James 已 import→改成上面 fcpxmld）。

## 交付內容（v5 結構）

- 底層：4K **原檔**＋拼貼 grid（transform 排版，**不重壓**）。
- 拼貼校正：16:9＋直式 → X 軸 **±640px（FCPXML 原始值 ±59.26，÷10.8 單位坑）**、**scale 1.0（James 要留框不放大）**、不 crop。三格/兩格/單片都處理。
- 三段疊層（都 70% 透明）：**票卡（左上，整段）** ＋ **地圖（右上，段首 4s 動線 draw-on 後定格）**。① 慕→林 ② 林→鵝 ③ 鵝→慕（Oberammergau 歸②）。
- 片尾 logo。
- **拍立得過場**：`overlays/_polaroid_linderhof.mov`（透明 ProRes4444，**5.0s**，四張林德霍夫照片掉落散落＋手寫地名）。

## 素材位置

- 疊層成品：`…/_初剪_day2_移動中/overlays/`（票卡 PNG×3、地圖 draw-on mov×3＋final png×3、polaroid mov、logo、mapcard/info）。
- **產生器腳本＋資料**：`…/_初剪_day2_移動中/_build/`（scratchpad 會清，已備份到此）
  - `build_fcpxml5.py`（組裝）、`render_bus_ticket.py`＋`gen_tickets.py`（票卡）、`render_map_card.py`（地圖動線）、`render_polaroid.py`（拍立得）、`manifest.json`／`selection.json`（39 片移動中資料）、`ai_map_bavaria.jpg`（James AI 地圖原圖）、`warm_film.cube`（暖膠捲 LUT，James 最後沒套走原色）。
- 照片：`…/_初剪_day2_移動中/photo/`（4 張林德霍夫）。

## 待辦（非急）

1. **narrative-tag-tool 巴士版型**：已上「①-B 遊覽車票＋路線地圖動線」＋PR-002 prompt 連結（已發佈）。✅
2. **FCP 批次套 LUT 教學**：James 這次已自己套色（126 處）→ 可能不需要；他要再問再教。
3. 匯出格式已確認：**H.264 / 1920×1080 / Rec.709 / AAC**（Apple 裝置 1080p 預設）＝通用成品；母帶留 fcpxmld＋原檔。見 reference_mp4_encoding_lr。

## 重開後怎麼接

- 要重出 fcpxml：`cd _build && python3 build_fcpxml5.py`（會覆蓋 `…/day2_移動中_初剪.fcpxml`）。⚠️但 James 的 fcpxmld 才是現行母帶，別覆蓋他的。
- 改票卡/地圖/拍立得：跑對應 render 腳本（venv：`/Users/james/.local/skills-src/pixel2motion/.venv/bin/python`）。
- DTD 驗證：`xmllint --noout --dtdvalid FCPXMLv1_10.dtd`（cd 進 FCP Interchange DTD 目錄）。
